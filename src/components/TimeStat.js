import React from 'react';

const TimeStat = (props) => {
  const { takenSec, doneTime } = props;

  return (
    <div className="">
      <div></div>
      <div></div>
    </div>
  );
};

export default TimeStat;
